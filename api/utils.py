#from reqwest import GET
import csv
import pathlib


def verify_image(url: str) -> bool:
    # make a request to the endpoint 
    #req = GET(url)
    
    #if req.status != 200:
        #return False
    return True
